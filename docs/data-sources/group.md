---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "keycard_group Data Source - keycard"
subcategory: ""
description: |-
  Reads a Keycard group. A group is a zone-scoped collection of users that can be assigned roles and referenced in policies.
---

# keycard_group (Data Source)

Reads a Keycard group. A group is a zone-scoped collection of users that can be assigned roles and referenced in policies.

## Example Usage

```terraform
# Look up a group by ID
data "keycard_group" "engineering" {
  zone_id = keycard_zone.production.id
  id      = keycard_group.engineering.id
}

# Look up a group by identifier, for groups created outside Terraform
data "keycard_group" "oncall" {
  zone_id    = keycard_zone.production.id
  identifier = "oncall-engineers"
}

# Assign a role to the group found by identifier
resource "keycard_group_role_assignment" "oncall_viewer" {
  zone_id         = keycard_zone.production.id
  group_id        = data.keycard_group.oncall.id
  role_identifier = "zone_viewer"
  owner_type      = "platform"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `zone_id` (String) The zone this group belongs to.

### Optional

- `id` (String) Unique identifier of the group. Either `id` or `identifier` must be provided, but not both.
- `identifier` (String) User-specified identifier for the group, unique within the zone. Either `id` or `identifier` must be provided, but not both.

### Read-Only

- `name` (String) Human-readable name for the group.
